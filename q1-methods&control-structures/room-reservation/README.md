# Room-Reservation

## Core Concept

- Composition
- ArrayList

## Time Complexity

- `requestRoom(String guestName)`: **O(n)**, where `n` is `rooms.length`.
- `cancelAndReassign(Reservation res)`: **O(m)**, where `m` is `waitList.size()`.