# Bird-Feeder

## Core Concept

- To form a random integer between 10 and 50 (inclusive), we need:

```java
int value = (int) (Math.random() * 41) + 10;
```
- if else logic

## Time Complexity

- `simulateOneDay()`: **O(1)** 
- `simulateManyDays()`: **O(n)** (let `n` be `numDays`)

