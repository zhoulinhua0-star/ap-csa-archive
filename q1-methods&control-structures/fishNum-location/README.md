# fishNum-location

# Core Concept

Composition: One object contains another object as an instance variable. Essentially it's a "has a" relationship among java classes.

# Time Complexity

- `numUnder()`: **O(1)** 
- `randomLocation()`: **O(1)**