# Grid-Location

## Core Concept

- Properly recognize the definition of what should be returned.
- if else logic

## Time Complexity

- `getNextLoc(int row, int col)`: O(1)
- `sumPath(int row, int col)`: O(R + C) where R = `grid.length` and C = `grid[0].length`