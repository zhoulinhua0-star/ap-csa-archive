# Cookie-variety

## Core Concept

- Traverse the arraylist using for loop

- Composition

## Time Complexity

- `getTotalBoxes()`: **O(n)**, where `n` is `orders.size()`

- `emoveVariety(String cookieVar)`: **O(n^2)**, because the call of `remove()` (removing elements at specifc index within the list) means extra work proportional to list size