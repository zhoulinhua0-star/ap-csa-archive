# Trail-prob

## Core Concept

- `Math.abs()` method

- use for loop to find max and min within the array at given index

## Time Complexity

- `isLevelTrailSegment(int start, int end)`: **O(n)**, where `n` is `end - start`

- `isDifficult`: **O(m)**, where `m` is `markers.length`