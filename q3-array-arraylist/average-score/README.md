# Average-Score

## Core Concept

Traversing the array: for loop

## Time Complexity

- `average(int first, int last)`: **O(n)**, where `n` is `last - first + 1`
- `hasImproved()`: **O(m)**, where `m` is `scores.length`
- `finalAverage()`: **O(m)**